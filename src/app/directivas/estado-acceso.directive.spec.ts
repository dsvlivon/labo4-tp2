import { EstadoAccesoDirective } from './estado-acceso.directive';

describe('EstadoAccesoDirective', () => {
  it('should create an instance', () => {
    const directive = new EstadoAccesoDirective();
    expect(directive).toBeTruthy();
  });
});
